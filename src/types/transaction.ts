export interface Transaction {
  name: string;
  status: string;
  category: string;
  carbonFootprint: number;
  fees: number;
  type: string;
  amount: number;
  currency: string;
  createdAt: string;
  icon: string;
  brandPartner: string;
}
